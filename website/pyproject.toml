# Dependency pins live in requirements.txt (Vercel reads that file).
# Entrypoint only — do not add [project.dependencies] here or uv may bundle extra deps.
[tool.vercel]
entrypoint = "website.wsgi:application"
